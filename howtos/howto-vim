#!/bin/zsh

if [[ -x /bin/pump ]]; then
	TRAPEXIT()	{
		if [[ -x /bin/pump ]]; then
			pump --shutdown
		fi
	}
	eval $(pump --startup)
fi

# FIXME	make -j12

# FIXME	exit 0

#
[[ -d src ]] && cd src || true
rm -rf configure
set -v
configure -m vim							\
	--with-features=huge						\
	--with-tlib='curses tlib'					\
	--enable-pythoninterp=yes					\
	--enable-python3interp=yes					\
	--enable-tclinterp						\
	--enable-rubyinterp=yes						\
	--enable-luainterp=yes						\
	--enable-sniff							\
	--enable-fontset						\
	--disable-netbeans						\
	--enable-gui=auto						\
	--enable-cscope							\
	CFLAGS='-pipe -g -Os -D_FORTIFY_SOURCE=1'			\
	"$@"
exit
	--disable-nextaw-check						\
	--enable-athena-check						\
	--enable-gnome-check						\
	--enable-gtk2-check						\
	--enable-gui=athena						\
	--enable-gui=gnome						\
	--enable-gui=gtk2						\
	--enable-gui=nextaw						\
	--enable-motif-check						\
	--enable-neXtaw-check						\
	--enable-perlinterp=dynamic					\
