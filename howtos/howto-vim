#!/bin/zsh

if [[ -x /bin/pump ]]; then
	TRAPEXIT()	{
		if [[ -x /bin/pump ]]; then
			pump --shutdown
		fi
	}
	eval $(pump --startup)
fi

make -j12

exit 0

#
cd src
rm -rf configure auto/configure
configure -m vim							\
	--with-features=huge						\
	--enable-pythoninterp=yes					\
	--enable-tclinterp						\
	--enable-rubyinterp=yes						\
	--enable-luainterp=yes						\
	--enable-sniff							\
	--enable-fontset						\
	--disable-netbeans						\
	--enable-gui=auto						\
	CFLAGS='-pipe -g -Os -D_FORTIFY_SOURCE=1'			\
	"$@"
exit
	--enable-cscope							\
	--disable-nextaw-check						\
	--enable-perlinterp=dynamic					\
	--enable-gui=nextaw						\
	--enable-neXtaw-check						\
	--enable-motif-check						\
	--enable-gui=athena						\
	--enable-athena-check						\
	--enable-gui=gtk2						\
	--enable-gtk2-check						\
	--enable-gnome-check						\
	--enable-gui=gnome						\
	--with-tlib=ncurses						\
